# Optional local/vision engines.
-r requirements.txt

# OpenNSFW2 needs a backend extra. The tf-keras extra uses the legacy
# TensorFlow Keras path, which is the default supported local install here.
opennsfw2[tf-keras]>=0.18.0,<0.19
nudenet>=3.4.2,<4
ultralytics>=8.3.0,<9
pytesseract>=0.3.10
